apiVersion: v1
kind: Pod
metadata:
  name: pod-config
spec:
  containers:
  - name: nginx
    image: nginx
    # single key value ni refer cheyadaniki below process
    # env:
    # - name: course
    #   valueFrom:
    #     configMapKeyRef:
    #       name: shivdev
    #       key: course
    # - name: trainer
    #   valueFrom:
    #     configMapKeyRef:
    #       name: shivdev
    #       key: trainer
    # below process is total file with all keys and values
    envFrom:
    - configMapRef:
      name: shivdev
